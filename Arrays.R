###### Working with metrices ###### 
# Least use DS in R
# Similar to vector and matrix
# can take on number of dimension (greater than 2)

theArray <- array(1:12, dim = c(2, 3, 2)) # 3D Array
theArray
 
